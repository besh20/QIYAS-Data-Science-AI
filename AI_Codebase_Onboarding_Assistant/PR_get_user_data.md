# Fix get_user_data: address reported issue

## Summary

The function `get_user_data` interpolates the `user_id` directly into an SQL query string, which allows a malicious user to perform SQL injection, potentially crashing the app or leaking data. The fix uses parameterized queries to safely pass the user_id to the database engine and validates that the `user_id` is an integer before querying. This prevents injection and ensures only valid IDs are used. The cache key is also normalized to the validated integer.

## Bug report

> malicious user_id crashes the app or leaks data

## Change

**File:** `sample_code/example_functions.py`
**Symbol:** `get_user_data`
**Agent confidence:** high

### Before
```python
def get_user_data(user_id, db_connection):
    if user_id in _cache:
        return _cache[user_id]
    result = db_connection.execute(f"SELECT * FROM users WHERE id = {user_id}")
    _cache[user_id] = result
    return result
```

### After
```python
def get_user_data(user_id, db_connection):
    """Retrieve user data safely using parameterized queries.

    Args:
        user_id: The identifier of the user (expected to be an integer or
            a string representing an integer).
        db_connection: A DB-API compatible connection object.

    Returns:
        The result of the query (e.g., a row dict or tuple).
    """
    # Validate and sanitize the user_id to ensure it is an integer.
    try:
        user_id_int = int(user_id)
    except (TypeError, ValueError) as exc:
        raise ValueError("Invalid user_id supplied") from exc

    # Use the sanitized integer for caching.
    if user_id_int in _cache:
        return _cache[user_id_int]

    # Execute a parameterized query to avoid SQL injection.
    cursor = db_connection.execute(
        "SELECT * FROM users WHERE id = ?", (user_id_int,)
    )
    # Fetch the result (adapt as needed for the DB driver).
    result = cursor.fetchone() if hasattr(cursor, "fetchone") else cursor

    _cache[user_id_int] = result
    return result
```

## Review notes

This fix was proposed and applied by an autonomous agent (Stage 4/5 of this
project) after investigating the codebase via hybrid search. It has **not**
been tested beyond the agent's own reasoning - please review the diff and
run the project's tests (or add one) before merging.

---
*Generated by agent.py + apply_fix.py*
